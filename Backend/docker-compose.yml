version: "3.9"

services:
  chatbot:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./knowledge:/app/knowledge
    env_file:
      - .env
    restart: unless-stopped
